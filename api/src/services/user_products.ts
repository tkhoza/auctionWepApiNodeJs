export const getUserProductById = (req: any, res: any) => {

  return res.send({ error: true, message: 'reading' })
}

//
export const updateUserProduct = (req: any, res: any) => {

  return res.send({ error: true, message: 'updating' })
}
